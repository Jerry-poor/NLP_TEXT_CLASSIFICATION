"prompt": "你是一个精通NLP的研究助手，你需要请基于以下训练示例，提取待处理输入中的实体标签以及位置。目标:  从给定文本中，根据预定义的实体类型列表识别每句话中的所有实体，并识别实体之间的关系（有则返回实体以及对应信息，没有则返回[]）。步骤:1. 分析上方训练示例，理解每个示例中句子的标注格式以及实体信息；2. 对待处理输入中的每句话，识别其中所有实体（每句话可能存在多个、单个或没有实体）。对于每个实体，请提取以下信息：[entity_name]: 实体名称（string 类型，以单引号'实体'标注）；[order]: 实体在句子中是第几次出现（int类型），第一次则为0,第二次为1,以此类推 ；[entity_label]: 实体标签（string 类型，以单引号'实体标签'标注），必须为以下类型之一：['per', 'org', 'gpe', 'geo', 'tim', 'art', 'eve', 'nat']   其中：per = Person， org = Organization ，gpe = Geopolitical Entity       geo = Geographical Entity，tim = Time indicator，art = Artifact，eve = Event ，nat = Natural Phenomenon 。每个实体只允许有一个标签，将每个实体格式化为 [entity_name, order, entity_label]  3. 格式化输出:   请将结果整理为字典格式，其中键为待处理输入中每句话的前10个字符（包括所有空白字符,需要进行转义处理），值为该句中所有实体信息的列表。   返回的字典条目顺序应与待处理输入中句子的原始顺序一致，格式示例如下：   { \'aaaaaaaaaa\': [[entity_name0, order, entity_label0], [entity_name1, order, entity_label1]], \'bbbbbbbbbb\': [[entity_name0, order, entity_label0], [entity_name1, order, entity_label1]]}请严格按照以上要求返回格式化后的结果，并确保输出为一个合法的字典字符串，不要返回任何空白或者换行符号"